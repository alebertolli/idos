Investment Decision Operating System (IDOS)
Software Design Document (SDD)
14: CI/CD Pipeline & Operational Workflows
Versión: 1.4
Estado: Aprobado

1. Objetivo
Describir el pipeline de CI/CD, el workflow operativo diario, semanal y mensual, y la estrategia de despliegue del IDOS.

2. Estrategia de CI/CD

2.1 Integración Continua (CI)
Todo commit a `main` ejecuta automáticamente:
- Linting (ruff)
- Tests unitarios (pytest, ~360 tests)
- Verificación de tipos (mypy, opt-in progresivo en módulos core)
- Validación de schemas YAML de `idos-config/`

2.2 Despliegue (CD)
El sistema se ejecuta localmente en el equipo del usuario. No hay despliegue a servidores remotos. Las actualizaciones se distribuyen via `git pull` desde el repositorio `idos`.

3. Workflows Operativos

3.1 Workflow Diario
8:00 - Market Open — Health Check: Verifica conectividad a fuentes de datos y brokers.
8:15 - Data Ingestion: Workers de datos ingieren noticias, precios, y eventos overnight.
8:30 - Market Observatory: Workers de Research analizan el mercado y generan alertas.
9:00 - Alert Review: El usuario revisa las alertas del Notification Center y toma decisiones.
15:30 - Market Close — Portfolio Update: Workers actualizan cartera y generan reporte diario.
16:00 - Daily Journaling: Workers registran eventos del día en `idos-journal/`.

3.2 Workflow Semanal
Lunes 12:00 — Weekly Strategy Review: Workers presentan análisis semanal.
Viernes 16:00 — Weekly Post-Mortem: Workers generan post-mortem de la semana.

3.3 Workflow Mensual
Día 1 — Monthly Portfolio Rebalance: Workers ejecutan rebalanceo mensual.
Día 1 — Monthly Report: Workers generan reporte mensual de rendimiento.
Día 5 — Conviction Review: Workers revisan y actualizan niveles de convicción.

3.4 Workflow Ad-Hoc
- Event-Driven (market): Eventos de mercado (earnings, macro data, breaking news).
  - quarterly-results: El Daily Refresh detecta earnings_date vencido en
    `idos-config/events/earnings.yml` y dispara `repository_dispatch` al
    DDD Pipeline, que reprocesa el ticker en FORCE mode (sin cambiar status).
- Event-Driven (user): Manual desde GitHub Actions.
  - DDD Pipeline acepta `workflow_dispatch` con inputs: `ticker`, `force`.
    FORCE mode: procesa cualquier estado, regenera research + assessments,
    no modifica el status de la oportunidad.
- Board Decision: El usuario convoca al Board Worker para una decisión específica.
- Manual: El usuario ejecuta Workers a demanda vía CLI.

3.5 GitHub Actions Pipelines

El sistema opera 3 pipelines en GitHub Actions:

| Pipeline | Trigger | Función |
|----------|---------|---------|
| **Monthly Universe Pipeline** | `schedule` día 1, `workflow_dispatch` | Finviz → Filter → Fetch → Scout → Opportunities |
| **DDD Research Pipeline** | `schedule` día 2, `workflow_run` (Universe), `workflow_dispatch`, `repository_dispatch` (quarterly-results) | ResearchWorker → AssessmentEngines → Conviction → Rules → Board → Entry → Notifications |
| **Daily Data Refresh** | `schedule` lun-vie 8am, `workflow_dispatch` | Fetch financiero + benchmark (SPY) + refresh Buy List + Entry Monitor + detección de earnings → dispatch quarterly-results |

El **Daily Data Refresh** ejecuta en orden: (1) refresh de datos financieros y benchmark SPY, (2) refresh de la Buy List (`BuyListRefreshWorker` actualiza `target_price`/`buy_zone_top`), (3) Entry Monitor sobre la Buy List, (4) Paper Trader (ejecución), (5) Risk Monitor, (6) reporte de errores de datos. Los errores de datos se consolidan en `cache/data_errors.json` y se reportan como UN issue diario (`⚠️ IDOS: errores de datos [fecha]`) vía el proceso de Error Management (SDD-16 §18).

El **DDD Pipeline** es multi-trigger con dos modos:
- **NORMAL**: solo procesa oportunidades DISCOVERED, transiciona a UNDER_DEEP_DD
- **FORCE**: procesa cualquier status (vía `force=true` o event `quarterly-results`), regenera assessments pero no cambia status

La configuración de earnings se mantiene en `idos-config/events/earnings.yml`:
```yaml
tickers:
  AAPL:
    earnings_date: "2026-07-25"    # Próximo earnings report
    triggered_at: null              # Se actualiza automáticamente al disparar
```

4. Roles y Responsabilidades

4.1 Workers Programados
Se ejecutan en intervalos definidos en `idos-config/schedules/`:
- `health_check_worker.py`: Verifica APIs y conectividad.
- `market_observatory_worker.py`: Análisis diario del mercado.
- `data_ingestion_worker.py`: Carga de datos externos.
- `portfolio_worker.py`: Gestión de cartera.
- `journal_worker.py`: Registro de eventos en diario.
- `postmortem_worker.py`: Análisis post-mortem.

4.2 Workers Event-Driven
Se ejecutan en respuesta a eventos del bus o via GitHub Actions:
- `research_worker.py`: Análisis fundamental/técnico de un ticker.
  - Opera en modo **NORMAL** (DISCOVERED → UNDER_DEEP_DD) o **FORCE** (skip can_transition, no cambia status).
  - FORCE mode recibe `force_reprocess=True` + `event_type` en el context.
- `board_worker.py`: Síntesis de decisiones de inversión.
- `scout_worker.py`: Identificación de oportunidades de mercado.

4.3 Workers de Infraestructura
- `git_queue_worker.py`: Serializa escrituras a Git (CommitRequest).
- `notification_worker.py`: Gestiona alertas al usuario.
- `checkpoint_manager_worker.py`: Checkpointing y recovery.

5. Estrategia de Testing

5.1 Tests Unitarios
Cubren Workers, módulos core, SDK, y utilidades. Se ejecutan con pytest. Ubicación: `idos-core/tests/`.

5.2 Tests de Integración
Cubren flujos cross-module y Event Bus. Ubicación: `idos-core/tests/test_integration/`.

6. SDK Packaging
El paquete `idos-core/` se instala en modo editable (`pip install -e .`). El SDK se importa como `idos.sdk`. Dependencias especificadas en `pyproject.toml`.

---

## Historial de Cambios

| Versión | Fecha | Cambios |
|---------|-------|---------|
| 1.5 | 2026-08-03 | Daily Data Refresh ampliado: benchmark SPY, refresh de Buy List, Entry Monitor y reporte consolidado diario de errores de datos (SDD-16 §18). Filtro de entrada 100% algorítmico (SDD-9 §5.1). |
| 1.4 | 2026-07-21 | Agregado: DDD Pipeline multi-trigger (schedule, workflow_run, workflow_dispatch con force, repository_dispatch quarterly-results). FORCE mode para reprocesamiento sin cambio de status. Daily Refresh con detección de earnings y dispatch. Config `idos-config/events/earnings.yml`. SDD-14 alineado con implementación real GHA. |
| 1.3 | — | Versión inicial del documento |
