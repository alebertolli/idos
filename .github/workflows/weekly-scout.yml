name: Weekly Scout

on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

jobs:
  scout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install
        run: |
          pip install -e idos-core
          pip install pydantic pyyaml requests beautifulsoup4 lxml yfinance schedule
      - name: Run scout
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          IDOS_TELEGRAM_BOT_TOKEN: ${{ secrets.IDOS_TELEGRAM_BOT_TOKEN }}
          IDOS_TELEGRAM_CHAT_ID: ${{ secrets.IDOS_TELEGRAM_CHAT_ID }}
        run: |
          python -c "
          from idos.workers.data.scout_worker import ScoutWorker
          import os
          os.chdir(os.environ.get('GITHUB_WORKSPACE', '.'))
          w = ScoutWorker()
          r = w.execute({'tickers': ['GOOGL','AAPL','MSFT','AMZN','META','NVDA','MELI','TSM','ASML','ISRG'], 'refresh_data': True})
          print(f'Screened: {r.output.get(\"tickers_screened\", 0)}')
          print(f'Passed: {r.output.get(\"passed_count\", 0)}')
          for res in r.output.get('results', []):
              print(f\"  {res['ticker']}: {res['score']} passed={res['passed']}\")
          "
      - name: Notify
        run: |
          python -c "
          from idos.workers.notifications.telegram import TelegramNotifier
          import os
          token = os.environ.get('IDOS_TELEGRAM_BOT_TOKEN')
          chat = os.environ.get('IDOS_TELEGRAM_CHAT_ID')
          if token and chat:
              t = TelegramNotifier({'bot_token': token, 'chat_id': chat})
              t.execute({'message': 'Weekly scout complete — check watchlist for new opportunities'})
              print('Telegram notification sent')
          else:
              print('Telegram not configured, skipping notification')
          "
